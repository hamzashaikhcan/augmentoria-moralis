{
	"name": "augmentoria_backend",
	"version": "1.0.0",
	"description": "Augmentoria Backend for Upload Media Files",
	"main": "app.js",
	"scripts": {
		"serve": "nodemon app.js",
		"start": "node app.js",
		"test": "echo \"Error: no test specified\" && exit 1",
		"build": "cd ../react && npm run build && cp -r build ../backend/client"
	},
	"author": "Hamza Zahid",
	"license": "ISC",
	"dependencies": {
		"axios": "^1.1.3",
		"body-parser": "^1.20.1",
		"cors": "^2.8.5",
		"express": "^4.18.2",
		"multer": "^1.4.5-lts.1",
		"nodemon": "^2.0.20"
	}
}
